interface:
  display_name: "Project Bootstrap"
  short_description: "Create projects from reusable local templates and standards"
  default_prompt: "Use $project-bootstrap to inspect or create a project from the local templates."
